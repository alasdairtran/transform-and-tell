dataset_reader:
  type: goodnews
  tokenizer:
    type: word
    word_splitter: just_spaces
  token_indexers:
    roberta:
      type: roberta
      model_name: roberta-base
      namespace: bpe
      padding_on_right: true
      padding_value: 1
  image_dir: data/goodnews/images_processed
  lazy: true
train_data_path: train
validation_data_path: val
test_data_path: test
vocabulary:
  type: roberta
  directory_path: ./expt/vocabulary
model:
  type: baseline
  use_context: true
  evaluate_mode: false
  topk: 1
  vocab_size: 50265
  hidden_size: 1024
  attention_dim: 1024
  namespace: bpe
  index: roberta
  padding_value: 1
  initializer:
    - - ^(attention|article_attention|init|f).*weight
      - type: xavier_uniform
    - - ^(attention|article_attention|init|f).*bias
      - type: zero
    - - ^rnn_cell.*weight
      - type: xavier_uniform
    - - ^rnn_cell.*bias
      - type: zero
iterator:
  type: bucket
  sorting_keys:
    - - context
      - num_fields
    - - context
      - list_num_tokens
    - - caption
      - num_tokens
  batch_size: 8
  max_instances_in_memory: 4096
  biggest_batch_first: false
  instances_per_epoch: 16384
  # maximum_samples_per_batch: ["total_num_tokens", 20000]
validation_iterator:
  type: bucket
  sorting_keys:
    - - context
      - num_fields
    - - context
      - list_num_tokens
    - - caption
      - num_tokens
  batch_size: 8
  max_instances_in_memory: 4096
  # maximum_samples_per_batch: ["total_num_tokens", 20000]
  biggest_batch_first: false
trainer:
  type: callback_apex
  apex_opt_level: O2
  keep_batchnorm_fp32: None
  optimizer:
    type: adam_w
    lr: 0.0001
    betas: [0.9, 0.999]
    eps: 0.000001
    weight_decay: 0.00001
    correct_bias: true
    parameter_groups:
      - - - ^attention
        - {}
      - - - ^init
        - {}
      - - - ^f
        - {}
      - - - ^rnn_cell
        - {}
  no_grad:
    - ^resnet
    - ^roberta
  num_epochs: 40
  shuffle: true
  cuda_device: 0
  callbacks:
    - type: checkpoint
    - type: track_metrics
      patience: 500
    - type: validate
    - type: log_to_tensorboard
      summary_interval: 500
      should_log_parameter_statistics: false
      log_batch_size_period: 5000
    - type: update_learning_rate
      learning_rate_scheduler:
        type: slanted_triangular
        num_epochs: 40
        num_steps_per_epoch: 2048
        cut_frac: 0.1
        ratio: 32
        gradual_unfreezing: false
        discriminative_fine_tuning: false
        decay_factor: 0.38
